#include<stdio.h>
int main()
{
    int a; 
    int b;
    int sum;
    printf("what is the vale of a: ");
    scanf("%d", &a);
    printf("What is the value of b: ");
    scanf("%d", &b);
    sum=a+b;
    printf("total Sum=%d",sum);
    return 0;
}